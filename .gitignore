node_modules/
.next/
dist/
build/
coverage/
target/
.vercel/
.env
.env.*
!.env.example
*.tsbuildinfo
.DS_Store
Thumbs.db

.vercel

.vercel

# Generated at CI build time by desktop-release.yml when Windows signing is
# configured (adds bundle.windows.signCommand via --config); never committed.
apps/desktop/src-tauri/app/tauri.signing.windows.json

# Real observerd binary is bundled at CI build time via bundle.resources (Task 1.6);
# never committed to git.
apps/desktop/src-tauri/app/binaries/
