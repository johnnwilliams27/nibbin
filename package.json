{
  "name": "nibbin",
  "private": true,
  "version": "0.1.0",
  "description": "Nibbin — AI agents that nibble your busywork away.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit -p packages/creatures && tsc --noEmit -p packages/shared && tsc --noEmit -p packages/connectors && tsc --noEmit -p packages/router && tsc --noEmit -p packages/keeper && tsc --noEmit -p packages/redaction && tsc --noEmit -p packages/runtime && tsc --noEmit -p packages/scan && tsc --noEmit -p packages/drip && tsc --noEmit -p packages/email && tsc --noEmit -p packages/channels && tsc --noEmit -p apps/web && tsc --noEmit -p apps/admin && tsc --noEmit -p apps/desktop && tsc --noEmit -p tests/redaction-corpus && tsc --noEmit -p tests/rls && tsc --noEmit -p tests/evals",
    "lint": "eslint .",
    "test": "vitest run",
    "audit": "npm audit --omit=dev --audit-level=high",
    "build": "npm run build -w apps/web && npm run build -w apps/admin",
    "grovemap": "node tools/grovemap/grovemap.mjs",
    "drip:worker": "tsx packages/drip/src/run.ts",
    "evals": "vitest run --config vitest.evals.config.ts",
    "eval:routing": "tsx packages/router/eval/run.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "^8.20.0",
    "eslint": "^9.17.0",
    "fast-check": "^4.8.0",
    "pg": "^8.21.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.18.0",
    "vitest": "^4.1.8"
  }
}
