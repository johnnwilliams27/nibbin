{
  "name": "@trust-index/collectors",
  "version": "0.1.0",
  "description": "Collectors that turn things in the world into Subjects: probe an endpoint, read a registry, emit observations the shared rating engine can score.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./mcp": "./src/mcp/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.123.0",
    "@trust-index/db": "workspace:*",
    "@trust-index/scoring": "workspace:*",
    "@trust-index/types": "workspace:*",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "playwright": "1.56.0",
    "tsx": "4.20.6",
    "typescript": "5.7.3",
    "vitest": "3.2.4"
  }
}
