{
  "name": "@trust-index/indexer",
  "version": "0.1.0",
  "description": "Chain access, backfill, metadata resolution, and continuous polling for the Agent Trust Index. Chain access is abstracted behind ChainSource; every test runs against a simulated provider.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@trust-index/types": "workspace:*",
    "viem": "2.56.0"
  },
  "devDependencies": {
    "@trust-index/db": "workspace:*",
    "@types/node": "26.4.0",
    "typescript": "5.7.3",
    "vitest": "3.2.4"
  }
}
