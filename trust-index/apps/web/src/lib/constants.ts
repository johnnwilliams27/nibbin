import { DEFAULT_CONSTANTS } from "@trust-index/types";

/** Single source for the methodology version string this app reports (SPEC 13 meta). */
export const METHODOLOGY_VERSION = DEFAULT_CONSTANTS.methodology_version;
