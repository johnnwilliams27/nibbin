export default function NotFound() {
  return (
    <main className="shell">
      <h1>Not found</h1>
      <p>Nothing is recorded at this address. Check the chain slug and identifier, or return home.</p>
      <p>
        <a href="/">Back to the index</a>
      </p>
    </main>
  );
}
