# supabase/migrations

Versioned schema + RLS migrations, managed with the Supabase CLI. First real
migration lands at M1 (account hierarchy: accounts/users/memberships, credit
ledger). Never make dashboard-only schema changes (docs/AGREEMENTS.md).
