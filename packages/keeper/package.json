{
  "name": "@nibbin/keeper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Grovekeeper conversation core (SPEC §4.2) — expression states, card system, onboarding script. No hands by construction (C10): this package exports pure functions only, never tools.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nibbin/router": "*"
  }
}
