/**
 * @nibbin/connectors — connector platform.
 *
 * Built at M3 (vault, aggregator integration, Tier-1 hand-built set, generic
 * MCP/IMAP rails). See .claude/skills/connector-builder before adding any
 * connector. C8/C9 (read-only scopes, vaulted tokens) are non-negotiable.
 */
export {};
