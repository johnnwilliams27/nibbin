{
  "name": "@nibbin/redaction",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Observer redaction core: shared rule data (regex battery, category blocklist) + the canonical 4-layer pipeline. The Rust daemon embeds rules/*.json via include_str!; edit rules here, never fork them.",
  "exports": {
    ".": "./src/index.ts",
    "./rules/redaction-rules.json": "./rules/redaction-rules.json",
    "./rules/category-blocklist.json": "./rules/category-blocklist.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
