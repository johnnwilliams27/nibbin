<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened-runtime entitlements for the notarized Developer ID build (§5.9).
  The webview (WKWebView) needs JIT + executable memory; library validation is
  relaxed so the bundled webview/runtime dylibs load under the hardened runtime.
  Capture permissions (screen recording / accessibility) are TCC runtime grants,
  not entitlements — the daemon requests them at first run.
-->
<plist version="1.0">
<dict>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
