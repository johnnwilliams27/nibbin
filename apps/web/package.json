{
  "name": "@nibbin/web",
  "version": "0.1.0",
  "private": true,
  "description": "Day One web app — grove, Grovekeeper chat, Agent Shop.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit"
  },
  "//": "typescript + @types/node are runtime deps (not devDeps) so Vercel's production install (NODE_ENV=production) keeps them — Next's build-time TS setup check requires the typescript package present even with typescript.ignoreBuildErrors.",
  "dependencies": {
    "@nibbin/creatures": "*",
    "@nibbin/keeper": "*",
    "@nibbin/router": "*",
    "@nibbin/shared": "*",
    "@supabase/ssr": "^0.12.0",
    "@supabase/supabase-js": "^2.108.1",
    "@types/node": "^22.10.0",
    "next": "^15.3.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "stripe": "^22.2.0",
    "typescript": "^5.7.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  }
}
