# Gate — Bug cleanup: sweep (#146, #133)

**Date:** 2026-06-20
**Branch:** `fix/bug-sweep`
**Scope:** #146 landing copy reconcile (90d→12mo); #133 make `claim_gmail_sweep` ownership authoritative in the RPC.
**Sensitive path:** `supabase/migrations/` (the #133 RPC) → gate required.

## Verdict: PASS (controller review)

#133 is a small, security-positive change reviewed directly by the controller.

### #133 — RPC ownership guard (security)
Migration `20260620140000_claim_sweep_ownership.sql` redefines `claim_gmail_sweep` to add a DB-level ownership guard at the top: the claim proceeds only when `EXISTS (connections WHERE id=_connection_id AND account_id=_account_id AND status='active')`, else it raises `insufficient_privilege`. This closes the previously app-only-guarded hole where a mis-wired/compromised caller could claim a sweep for a connection not belonging to the account.
- **Fail-closed:** missing/inactive/mismatched connection → exception, no DML.
- **No behavior change otherwise:** signature, `uuid` return (null=skip), `SECURITY DEFINER`, `search_path=public`, `service_role`-only grant, the `derived_written_at` provenance auto-finalize, the on-conflict stale-reclaim, and NULL-skip semantics are all preserved verbatim.
- **Privilege:** the function is `service_role`-only (revoked from public); the guard is defense-in-depth even though the caller is already trusted server code.
- Applied + validated on dev/staging/prod (valid SQL, function replaced).

### #146 — copy only (non-sensitive)
`apps/web/app/page.tsx` three "90 days"/"90-day" references → "the last 12 months"/"12-month scan", matching the in-app consent copy and the real `SWEEP_WINDOW_DAYS=365`. No logic change.

## Accepted / notes
No raw-data, egress, or RLS change. The ownership guard only narrows what the RPC will do; it cannot widen access.
