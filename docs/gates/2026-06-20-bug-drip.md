# Gate — Bug cleanup: drip (#39, #40, #41, #42, #45)

**Date:** 2026-06-20
**Branch:** `fix/bug-drip`
**Sensitive paths:** `packages/drip/`, `supabase/migrations/` → gate required.

## Verdict: PASS (controller review)

Five drip-subsystem gate findings, each a bounded correctness fix; 37/37 drip tests pass, lint clean.

- **#45** — `buildDiagnosisReveal` now a correct three-way branch on study lifecycle: `studyCompleted` → diagnosis-ready, `studyActive` → watching, else → Field Study pitch (was branching on newest-event `studyActive` only). `studyCompleted` exposed on `ArcFlags`.
- **#42** — over-long `nibbins.name` could throw in the drip worker and brick an account's drip for 30 days. Migration `20260620150000_nibbin_name_length_cap.sql` (renumbered from a colliding 20260620140000) truncates existing rows then adds a `char_length(name) <= 150` CHECK (the column is now the authority); worker loop adds per-event try/catch + continue so one bad event can't abort the tick. Applied dev/staging/prod.
- **#41** — 20h spacing-floor race: `claimSend` now wraps the claim in `private.lock_account(account)` advisory lock (signature confirmed against the M4 migration) so per-account claims serialize.
- **#40** — tz guard made case-insensitive (`lower()` both sides) so non-canonical-cased zones no longer silently fall back to UTC and shift day-1 boundaries.
- **#39** — `insertEarnedNotification` returns a boolean; the worker counts `earnedNotifications` only when actually inserted, so the rolling-window query no longer inflates counts / re-celebrates old evolutions.

## Notes / accepted
- Migration ordering: the name-cap migration must precede the worker deploy to fully close the long-name window; the worker's defensive title-truncation covers the interim. (Both ship in this PR.)
- #41's race-closure unit test asserts the SQL shape, not true concurrency (the in-memory test store can't replicate Postgres advisory-lock serialization). DB-level fix is correct; full confirmation needs a Postgres integration harness.
